import 'package:flutter/material.dart';

class HesapAyalari extends StatelessWidget {
  const HesapAyalari({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}