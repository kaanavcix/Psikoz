import 'package:flutter/material.dart';

class Sozlesmeler extends StatelessWidget {
  const Sozlesmeler({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}