import 'package:flutter/material.dart';

class ShadowConstants {
  static dynamic boxShadow = const
      BoxShadow(color: Colors.white, blurRadius: 0.2, offset: Offset(0, 1));
      static dynamic boxShadowDark = const
      BoxShadow(color: Colors.black, blurRadius: 0.2, offset: Offset(0, 1));

      
}
